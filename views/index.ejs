<!DOCTYPE html>
<html>
  <head>
    <title>Lisper</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&family=Titan+One&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" type="text/css" href="styles/main.css" />
  </head>

  <body>
    <h1>What's the joke?</h1>
    <form id="myForm" method="post" action="/post">
      <label for="idInput" style="color: red">Enter your name:</label>
      <input type="text" id="idInput" name="id" />
      <input id="get" type="submit" value="GET" />
    </form>
    s <% if(locals.joke1) { %>
    <div class="card">
      <p><%= joke1 %></p>
    </div>
    <p class="user"><%= joke2 %></p>
    <% } else { %>
    <div class="card"></div>
    <p class="user"><%= joke3 %></p>
    <% } %>
  </body>
</html>
